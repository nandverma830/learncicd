import {Dimensions} from 'react-native';
const deviceWidth = Dimensions.get('window').width;
const deviceHeight = Dimensions.get('window').height;
const deviceRatio = (deviceHeight + deviceWidth) / 2;
const boxHeight = (deviceRatio * 10) / 100;
const boxWidth = (deviceRatio * 9) / 100;
const iconSize = (deviceRatio * 4) / 100;
export default {
  pop_Regular: 'Poppins-Regular',
  pop_SemiBold: 'Poppins-SemiBold',
  pop_bold: 'Poppins-Bold',
  pop_Extrabold: 'Poppins-ExtraBold',
  pop_light: 'Poppins-Light',
  pop_medium: 'Poppins-Medium',
  lato_bold: 'Lato-Bold',
  lato_light: 'Lato-Light',
  lato_regular: 'Lato-Regular',
  deviceRatio: deviceRatio,
  deviceWidth: deviceWidth,
  deviceHeight: deviceHeight,
  font_size: (deviceWidth * 3.6) / 100,
  font_size1: (deviceWidth * 3.9) / 100,
  font_size2: (deviceWidth * 4.3) / 100,
  font_size3: (deviceWidth * 4.8) / 100,
  font_size4: (deviceWidth * 5.8) / 100,
  font_size5: (deviceWidth * 6.3) / 100,
  font_size6: (deviceWidth * 7.5) / 100,
  headingFont: (deviceWidth * 7.5) / 100,
  extraLarge: (deviceWidth * 9) / 100,
  miniFont: (deviceWidth * 2.5) / 100,
  miniFont1: (deviceWidth * 2.7) / 100,
  smallFont: (deviceWidth * 3) / 100,
  basicFont: (deviceWidth * 3.4) / 100,
  lineSpacing: (deviceWidth * 6) / 100,
  pdfImageHeight: (deviceHeight * 20) / 100,
  boxHeight: boxWidth,
  boxWidth: boxWidth,
  iconSize: iconSize,

  font: (deviceWidth * 2) / 100,
  font2: (deviceWidth * 2.3) / 100,
  font4: (deviceWidth * 2.6) / 100,
  font5: (deviceWidth * 2.9) / 100,
  font6: (deviceWidth * 3.2) / 100,
  font8: (deviceWidth * 3.5) / 100,
  font10: (deviceWidth * 3.8) / 100,
  font12: (deviceWidth * 4.1) / 100,
  font14: (deviceWidth * 4.4) / 100,
  font16: (deviceWidth * 4.7) / 100,
  font18: (deviceWidth * 5) / 100,
  font20: (deviceWidth * 5.3) / 100,
  font22: (deviceWidth * 5.6) / 100,
  font24: (deviceWidth * 5.9) / 100,
  font26: (deviceWidth * 6.2) / 100,
  font28: (deviceWidth * 6.5) / 100,
  font30: (deviceWidth * 6.8) / 100,
  font32: (deviceWidth * 7.1) / 100,
  font34: (deviceWidth * 7.4) / 100,
  font36: (deviceWidth * 7.7) / 100,
};
